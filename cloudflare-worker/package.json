{
  "name": "fitness-render-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker scaffold for offloading Remotion renders. NOT DEPLOYED.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.78.0"
  }
}
